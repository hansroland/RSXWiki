---
toc: no
...

# Using the LLVM Backend with GHC 

In the .cabal file use the line:

    ghc-options:       -fllvm -pgmlc /usr/lib/llvm-13/bin/llc -pgmlo /usr/lib/llvm-13/bin/opt

Change 13 according the GHC version!